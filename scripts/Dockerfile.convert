FROM python:3.11-slim

WORKDIR /convert

# Pin numpy<2 for tf2onnx compatibility, use newer tf2onnx
RUN pip install --no-cache-dir "numpy<2" tensorflow==2.18.0 tf2onnx==1.16.1 onnx requests

COPY convert_to_onnx.py .

CMD ["python", "convert_to_onnx.py"]
